#!/bin/bash
# SessionStart hook — instructs the agent to use a skill at the start of every session.
# Claude Code captures stdout as a system reminder before the first prompt.
#
# To enforce a different skill, copy this script and update the echo below.

echo "Use the agent-collaboration skill."
